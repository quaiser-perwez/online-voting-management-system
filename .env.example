# copy this file to .env and fill in the values before running the app
# keep the real .env out of source control (see .gitignore)

mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/voting_db
